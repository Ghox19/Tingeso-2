apiVersion: apps/v1
kind: Deployment
metadata:
  name: tracing-microservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tracing-microservice
  template:
    metadata:
      labels:
        app: tracing-microservice
    spec:
      containers:
      - name: tracing-microservice
        image: ghox19/tracing-microservice:latest
        ports:
        - containerPort: 8080
        env:
        - name: POSTGRES_DB_TRACING
          valueFrom:
            configMapKeyRef:
              name: postgres-config
              key: POSTGRES_DB_TRACING
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: POSTGRES_PASSWORD
        - name: POSTGRES_HOST
          value: "postgres"
        - name: POSTGRES_PORT
          value: "5432"
---
apiVersion: v1
kind: Service
metadata:
  name: tracing-microservice
spec:
  selector:
    app: tracing-microservice
  ports:
  - port: 80
    targetPort: 8080